---
"@chialab/vitest-provider-browserstack": minor
---

Report test results as the BrowserStack session status, free the concurrency slot as soon as each instance's tests finish, and let build/project names be set explicitly via `createBrowserStackProvider(data, options)`.
