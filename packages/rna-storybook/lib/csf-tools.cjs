// we need to require the module since its mjs version has incorrect import statements
// eslint-disable-next-line @typescript-eslint/no-var-requires
const csfTools = require('@storybook/csf-tools');

module.exports = csfTools;
